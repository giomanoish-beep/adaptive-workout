## V1-005 Remediation Plan

### Empirical baseline (validated this session)
- **1024/1024 unit tests pass.** `npm audit --omit=dev`: 0 vulnerabilities. `dist/` builds.
- **No Rules of Hooks crash exists.** `ActiveWorkout.tsx` already calls every hook at the top level in unconditional order; the debug spec shows `PAGE ERRORS:` empty and full active-workout HTML renders. The task's Phase 1 premise is stale.
- Real chromium E2E failures group into: (a) a11y `aria-progressbar-name`, (b) a11y `color-contrast` on bottom-nav labels, (c) timeouts because `setupWorkoutFlow`/`setupActiveWorkout` never install route mocks, (d) missing PWA manifest+icons.

---

### Phase 1 — Hooks: regression check only (NO CODE CHANGE)
Per your direction, treat the hooks diagnosis as stale. I will:
1. Inspect `ActiveWorkout.tsx` and confirm no hook is called conditionally (already done — confirmed clean).
2. Run `active-workout` + `workout-session` unit tests.
3. Run the active-workout E2E entry path (flow-2/flow-3).
4. Confirm zero React console/page errors via the debug spec output (already confirmed: `PAGE ERRORS:` empty).
5. Document Phase 1 as **NO CHANGE REQUIRED** in the final report.

Only the two cosmetic `as any` casts (lines 94, 133) get cleaned up via proper discriminated-union narrowing — no structural change, no child-component extraction.

---

### Phase 2 — E2E store contracts
No ActiveWorkout crash to unblock, and flow-3 (set logging, RIR, rest, finish, reload-resume via `preserveStoreAndReload`) already passes against the real `useWorkoutSession` → E2E Supabase adapter path. I will **verify** (not assume) each required contract is exercisable: session creation persists one row, session exercises persist, set-1 decimal weight + `rir:0`, set-2 blank RIR → `rir:null`, edit updates the row, reload resumes, finish updates status, history reads it. If flow-3 + V1-005-critical-journey already cover these (they do for the happy path), Phase 2 needs no new adapter code — only confirmation. RIR 0-vs-null evidence comes from Phase 6.

---

### Phase 3 — Accessibility (fix the app, not the tests)
1. **Progressbar accessible name** — `OnboardingFlow.tsx:80`: add `aria-label="Onboarding progress"` to the `role="progressbar"` div. Preserve `aria-valuemin/max/now`.
2. **Bottom-nav contrast** — `styles.css:129`: change `.bottom-nav__item { color: #64748b }` → `#94a3b8` (slate-400). Computed: 3.77:1 (FAIL) → 7.00:1 (PASS AA+). Active `#5eead4` already passes (12.13:1), untouched. Validated for both chromium (Desktop Chrome) and iPhone SE viewports via the existing a11y-audit + mobile-hardening specs.

---

### Phase 4 — PWA manifest + icons (original placeholder art)
Create `apps/web/public/`:
- `manifest.webmanifest` — name, short_name, description, `start_url "/"`, `scope "/"`, `display: "standalone"`, `background_color #0b1120`, `theme_color #0b1120`, 4 icon entries (192/512 any + maskable).
- `icons/icon-192.png`, `icon-512.png`, `icon-maskable-192.png`, `icon-maskable-512.png` — generated from an **original** SVG (dark `#0b1120` rounded-square background, white dumbbell/"AW" mark, content within the 80% maskable safe zone). I'll author the SVG inline and rasterize to PNGs via a small Node script using the `sharp` npm dependency if available, otherwise generate the PNGs directly with a hand-written minimal PNG encoder fallback is too heavy — **I'll generate them via a one-off Node script using the canvas-less approach: write the SVG, then use `npx sharp-cli` or, if sharp isn't installed, commit a tiny script that emits valid PNGs through the existing build tooling.** Final approach decided at execution time; the manifest + SVG + index.html link are the load-bearing parts for the `pwa-smoke` test (it only requires `icons[0].src` to return 200).
- `favicon.svg` — same mark.

Edit `apps/web/index.html`: add `<link rel="manifest" href="/manifest.webmanifest">`, `<link rel="icon" href="/favicon.svg" type="image/svg+xml">`, `<link rel="apple-touch-icon" href="/icons/icon-192.png">`. Keep existing `theme-color`.

Vite serves `public/` at `/` in both dev (Playwright webServer) and build → URLs resolve with no config change.

---

### Phase 5 — Clean temporary debug artifacts
- Delete `apps/web/e2e/debug-crash.spec.ts` and `apps/web/e2e/debug-start-workout.spec.ts`.
- Remove the unused imports causing web typecheck failures: `ActiveWorkoutState` in `ActiveWorkout.tsx:11`, `hasSW` in `pwa-smoke.spec.ts:67`.
- Keep `helpers.ts` / `route-mocks.ts` (reusable).

---

### Phase 6 — Error/retry E2E coverage (new spec)
Add `apps/web/e2e/error-retry.spec.ts` covering, using route-mock configs and **visible states / Playwright auto-waiting only (no fixed waits)**:
- Workout generation: first request fails (`generationFails:true`) → controlled alert visible → Retry succeeds → duplicate submission prevented (button disabled while in-flight).
- Set logging: invalid input can't complete (Complete disabled), decimal weight accepted, RIR 0 accepted, blank RIR → null (assert via the logged-set display `RIR —`), edit persists.
- Active workout: reload resumes (`preserveStoreAndReload`), rest timer changes (+15/Skip/Dismiss), incomplete finish requires confirmation.
- Progression: existing data stays visible while refreshing, first refresh fails (`progressionRefreshFails:true`) → Retry → duplicate refresh prevented → result or insufficient-data state.
- Settings: saved goal survives reload; save-error alert path if the mock supports it (verify; skip cleanly if not).

---

### Phase 7 — Production-seam guard
Add `scripts/test-e2e-production-guard.mjs`: runs `vite build` with `VITE_E2E_AUTH=true`, expects the build/biuld-time check in `App.tsx:17` to reject it (note: that check is runtime, not build-time — so the guard will instead **build, then grep the emitted `dist/assets/*.js` for the E2E seam being reachable in production**, failing if the seam code is included in a production bundle). Add npm script `test:e2e-production-guard` at repo root. Clean up any partial `dist/` left by a failed run.

*Refinement:* `App.tsx` throws at runtime in production if `VITE_E2E_AUTH==='true'`, but `createE2ESupabaseClient` is still imported unconditionally at `App.tsx:4`, so it ships in the production bundle. The guard will assert the build **succeeds** but that the production bundle either (a) does not contain `__E2E_STORE__` / `createE2ESupabaseClient` references, or (b) documents that the runtime guard is the actual gate. I'll verify empirically what `vite build` tree-shakes and make the guard assert the strongest true invariant.

---

### Phase 8 — Validation
Run, in order, and report each result:
1. `npx vitest run --no-file-parallelism` (focused: active-workout, workout-session, onboarding, navigation, then full suite)
2. `npm test`
3. `npm run typecheck` (fix the 2 unused-import errors)
4. `npm run build`
5. `npm audit --omit=dev`
6. `npm run edge-fn:build:all`
7. `npm run test:e2e-production-guard`
8. `npm run test:e2e -- --project=chromium`
9. `npm run test:e2e -- --project="iPhone SE"`
10. `npm run test:e2e`

---

### Test-helper refactor (Phase 3 enabler, per your spec)
Refactor `apps/web/e2e/helpers.ts`:
- Split into `installE2EEnvironment(page)` (route mocks) + `resetE2EState(page)` (goto + clear store + reload). `setupE2ETest` = both.
- Make `setupE2ETest` idempotent per-page via `WeakSet<Page>` (route handlers persist across reload anyway, so double-install is safe but we avoid redundant work).
- Move `setupWorkoutFlow` and `setupActiveWorkout` **into `helpers.ts`** (currently duplicated inline in a11y-audit.spec.ts and mobile-hardening.spec.ts) with signature `setupWorkoutFlow(page, { initialize: true })`. `setupActiveWorkout` calls `setupE2ETest` then `setupWorkoutFlow(page, { initialize: false })`.
- Add a regression test asserting `setupActiveWorkout` works with no prior manual setup.

---

### Files touched (summary)
- **Edit:** `apps/web/src/onboarding/OnboardingFlow.tsx` (aria-label), `apps/web/src/styles.css` (contrast), `apps/web/src/active-workout/ActiveWorkout.tsx` (remove 2 `as any` + unused import), `apps/web/index.html` (manifest links), `apps/web/e2e/helpers.ts` (refactor + moved helpers), `apps/web/e2e/a11y-audit.spec.ts` + `mobile-hardening.spec.ts` (use shared helpers), `apps/web/e2e/pwa-smoke.spec.ts` (unused var), `package.json` (new script), `docs/IMPLEMENTATION_PLAN.md`.
- **Create:** `apps/web/public/manifest.webmanifest`, `apps/web/public/favicon.svg`, `apps/web/public/icons/*.png`, `apps/web/e2e/error-retry.spec.ts`, `scripts/test-e2e-production-guard.mjs`, focused unit tests for hook-stability if Phase 1 stays no-change (lightweight: assert the component renders through loading→active→finished without throwing, via a render test if the tooling supports jsdom — otherwise document as E2E-covered).
- **Delete:** `apps/web/e2e/debug-crash.spec.ts`, `apps/web/e2e/debug-start-workout.spec.ts`.

### Known limitation to flag
The "hook-stability unit tests" the original task requests (Phase 1) are only meaningful if a hook bug exists. Since none exists, I'll add a lightweight render-smoke test if `@testing-library/react` is available; if not (web package.json has no test-render deps), I'll document hook-stability as covered by the passing E2E flow-2/flow-3 and the debug-spec's empty `PAGE ERRORS:`.