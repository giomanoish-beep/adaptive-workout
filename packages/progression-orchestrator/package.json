{
  "name": "@adaptive-workout/progression-orchestrator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "dependencies": {
    "@adaptive-workout/domain": "0.0.0",
    "@adaptive-workout/progression-engine": "0.0.0",
    "@adaptive-workout/progression-decision-persistence": "0.0.0"
  },
  "scripts": {
    "build": "npm run build --workspace @adaptive-workout/domain && npm run build --workspace @adaptive-workout/progression-engine && npm run build --workspace @adaptive-workout/progression-decision-persistence && tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit"
  }
}